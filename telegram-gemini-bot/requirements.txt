# Версии взяты из последнего успешного запуска GitHub Actions 13.08.2026.
# Обновляются отдельным PR через Dependabot, а не внезапно при каждом запуске.
pyTelegramBotAPI==4.36.0
# openai не пиновать точной версией: пакет живой SDK для многих провайдеров,
# а не сам xAI, и версия, увиденная на момент написания этого файла, могла
# устареть быстрее, чем до неё дойдёт Dependabot. Нижняя граница — минимум,
# в котором точно есть нужный синтаксис клиента (client.chat.completions).
openai>=1.50,<3
python-dotenv==1.2.2
requests==2.34.2
